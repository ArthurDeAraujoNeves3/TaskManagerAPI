meta {
  name: resetPassword
  type: http
  seq: 3
}

put {
  url: {{BASE_URL}}/users/resetpassword
  body: json
  auth: inherit
}

body:json {
  {
    "password": {{PASSWORD}},
    "newPassword": "teoremadepitagoras"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
