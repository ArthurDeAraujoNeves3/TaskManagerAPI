meta {
  name: create
  type: http
  seq: 1
}

post {
  url: {{BASE_URL}}/teams/create
  body: json
  auth: inherit
}

body:json {
  {
    "name": "Soror",
    "description": "Time formado pelos habitantes de Betelgeuse"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
