meta {
  name: create
  type: http
  seq: 1
}

post {
  url: {{BASE_URL}}/teams/create
  body: json
  auth: inherit
}

body:json {
  {
    "name": "Time teste",
    "description": "Descricao"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
