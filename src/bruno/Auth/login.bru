meta {
  name: login
  type: http
  seq: 2
}

post {
  url: {{BASE_URL}}/auth/login
  body: json
  auth: inherit
}

body:json {
  {
    "email": "ulyssemerou@gmail.com",
    "password":  {{PASSWORD}}
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
